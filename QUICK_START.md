# 🚀 Быстрый старт - JapJaryq Academy

## ✅ Что уже настроено:

1. **База данных**: PostgreSQL `edtech` с 12 таблицами
2. **Backend**: http://localhost:5001 (работает)
3. **Frontend**: http://localhost:3000 (работает)
4. **Многоязычность**: РУ/ҚЗ переключатель в Header

---

## 👤 Тестовые учетные данные

### Админ:
- **Email**: `admin@japjaryq.kz`
- **Пароль**: `admin123`
- **Роль**: ADMIN

---

## 🎯 Как войти в админ-панель:

1. Откройте http://localhost:3000
2. Нажмите **"Войти"** в правом верхнем углу
3. Введите:
   - Email: `admin@japjaryq.kz`
   - Пароль: `admin123`
4. После входа перейдите в **"Админ панель"** в меню
5. Или откройте напрямую: http://localhost:3000/admin

---

## 🌐 Проверка многоязычности:

1. На главной странице найдите переключатель **РУ / ҚЗ** в Header
2. Нажмите **ҚЗ** - весь контент переключится на казахский
3. Нажмите **РУ** - вернется русский
4. Выбор сохраняется в localStorage

---

## 📊 Доступные модули админки:

### ✅ Готовые:
- **Курсы** (`/admin/courses`) - создание и управление курсами
- **Уроки** (`/admin/lessons`) - управление уроками
- **Пользователи** (`/admin/users`) - управление пользователями
- **Платежи** (`/admin/payments`) - просмотр платежей
- **Доступы** (`/admin/access`) - управление доступами
- **Тесты** (`/admin/tests`) - создание тестов

---

## 🔧 Полезные команды:

### Запуск серверов:

```bash
# Backend
cd backend && npm run dev

# Frontend  
cd frontend && npm run dev
```

### Работа с БД:

```bash
# Подключиться к БД
psql -d edtech

# Посмотреть таблицы
\dt

# Посмотреть пользователей
SELECT email, role FROM users;

# Сделать пользователя админом
UPDATE users SET role = 'ADMIN' WHERE email = 'your@email.com';
```

### Prisma:

```bash
cd backend

# Синхронизировать схему с БД
npx prisma db push

# Открыть Prisma Studio
npx prisma studio
```

---

## 🐛 Решение проблем:

### Страница висит на загрузке:
- Проверьте что backend работает: `curl http://localhost:5001/health`
- Проверьте что БД доступна: `psql -d edtech -c "\dt"`

### Ошибка 401 Unauthorized:
- Войдите через `/auth/login`
- Используйте тестовые данные выше

### Ошибка доступа к БД:
- Проверьте `backend/.env`: `DATABASE_URL="postgresql://eracode@localhost:5432/edtech?schema=public"`
- Убедитесь что PostgreSQL запущен: `brew services list | grep postgresql`

### Бесконечные запросы 500:
- Остановите backend: `pkill -f "tsx watch"`
- Очистите кэш: `cd backend && rm -rf node_modules/.cache`
- Перезапустите: `npm run dev`

---

## 📚 Документация:

- **API**: `API_DOCUMENTATION.md` - все endpoints
- **i18n**: `I18N_GUIDE.md` - работа с переводами
- **Админка**: `ADMIN_GUIDE.md` - руководство администратора
- **Функции**: `FEATURES_SUMMARY.md` - обзор всех функций

---

## 🎉 Готово к работе!

Платформа полностью настроена и готова к использованию. Войдите как админ и начните добавлять курсы!

**Дата**: 31 декабря 2024  
**Версия**: 1.1.0

