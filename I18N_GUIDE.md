# 🌐 Руководство по многоязычности JapJaryq Academy

## 📌 Обзор

Платформа поддерживает **2 языка**:
- 🇷🇺 **Русский** (ru) - по умолчанию
- 🇰🇿 **Казахский** (kk)

Система построена на React Context API + localStorage для сохранения выбранного языка.

---

## 🗂️ Структура файлов

```
frontend/src/
├── contexts/
│   └── LanguageContext.tsx      # Context для управления языком
├── components/
│   └── LanguageSwitcher.tsx     # Переключатель языков (РУ/ҚЗ)
└── locales/
    ├── ru.json                  # Русские переводы
    └── kk.json                  # Казахские переводы
```

---

## 🚀 Как использовать

### 1. В компонентах

```tsx
'use client';

import { useLanguage } from '@/contexts/LanguageContext';

export default function MyComponent() {
  const { t, language, setLanguage } = useLanguage();

  return (
    <div>
      <h1>{t.hero.title}</h1>
      <p>{t.hero.description}</p>
      
      {/* Изменить язык */}
      <button onClick={() => setLanguage('kk')}>
        Қазақша
      </button>
    </div>
  );
}
```

### 2. Переключатель языков

Компонент `<LanguageSwitcher />` уже добавлен в Header:

```tsx
import LanguageSwitcher from '@/components/LanguageSwitcher';

// В Header
<LanguageSwitcher />
```

---

## 📝 Структура переводов

### Файл: `locales/ru.json` и `locales/kk.json`

```json
{
  "common": {
    "login": "Войти",
    "register": "Зарегистрироваться",
    "loading": "Загрузка...",
    ...
  },
  "nav": {
    "home": "Главная",
    "courses": "Курсы",
    ...
  },
  "hero": {
    "title": "Станьте профессиональным электриком",
    "description": "...",
    ...
  },
  ...
}
```

### Доступ к вложенным переводам:

```tsx
const { t } = useLanguage();

// Простое обращение
t.common.login

// Вложенные объекты
t.hero.title
t.benefits.practice.title
t.footer.contacts

// Массивы (для списков)
t.curriculum.module1.topics.map((topic) => ...)
```

---

## ✅ Уже переведены

### Frontend страницы:

- ✅ **Главная страница** (`/`)
  - Hero секция
  - Статистика
  - Преимущества (6 карточек)
  - Программа обучения (4 модуля)
  - Отзывы (3 отзыва)
  - CTA секция

- ✅ **Header**
  - Навигация
  - Кнопки входа/регистрации
  - Кнопка выхода
  - Переключатель языков

- ✅ **Footer**
  - Описание
  - Ссылки на курсы
  - Информация
  - Контакты

- ✅ **Страницы аутентификации**
  - `/auth/login` - Вход
  - `/auth/register` - Регистрация

---

## 🔄 Что нужно перевести дальше

### Высокий приоритет:

1. **Страница курсов** (`/courses`)
2. **Личный кабинет** (`/dashboard`)
3. **Админ панель** (`/admin/*`)

### Средний приоритет:

4. **Страница курса** (детальная)
5. **Страница урока** (проигрыватель видео)
6. **Сообщения об ошибках**
7. **Уведомления**

---

## 📚 Добавление новых переводов

### Шаг 1: Добавьте ключи в оба файла

**`locales/ru.json`:**
```json
{
  "courses": {
    "title": "Все курсы",
    "noCourses": "Курсы не найдены"
  }
}
```

**`locales/kk.json`:**
```json
{
  "courses": {
    "title": "Барлық курстар",
    "noCourses": "Курстар табылмады"
  }
}
```

### Шаг 2: Используйте в компоненте

```tsx
const { t } = useLanguage();

<h1>{t.courses.title}</h1>
```

---

## 🎨 Стиль переключателя

Переключатель языков имеет современный дизайн:

```tsx
<div className="flex items-center gap-1 bg-gray-100 rounded-lg p-1">
  <button className={language === 'ru' ? 'active' : ''}>РУ</button>
  <button className={language === 'kk' ? 'active' : ''}>ҚЗ</button>
</div>
```

Активный язык подсвечивается белым фоном с тенью.

---

## 💾 Сохранение выбранного языка

Язык автоматически сохраняется в `localStorage`:

```typescript
// При изменении языка
localStorage.setItem('language', 'kk');

// При загрузке страницы
const savedLanguage = localStorage.getItem('language');
```

---

## 🔧 Настройка TypeScript

Типы автоматически выводятся из `ru.json`:

```typescript
type Translations = typeof ru;

// Автокомплит работает для всех ключей
t.hero.title // ✅
t.common.login // ✅
t.invalid.key // ❌ Ошибка типа
```

---

## 🌍 SEO и метаданные

Для динамических метаданных в зависимости от языка:

```tsx
export async function generateMetadata(): Promise<Metadata> {
  // В будущем можно сделать динамическую генерацию
  return {
    title: 'JapJaryq Academy - Онлайн-школа электриков',
    description: '...'
  }
}
```

---

## 📊 Статистика переводов

| Раздел | Русский | Казахский | Статус |
|--------|---------|-----------|--------|
| Главная страница | ✅ 100% | ✅ 100% | **Готово** |
| Header | ✅ 100% | ✅ 100% | **Готово** |
| Footer | ✅ 100% | ✅ 100% | **Готово** |
| Аутентификация | ✅ 100% | ✅ 100% | **Готово** |
| Курсы | ⏳ 0% | ⏳ 0% | В планах |
| Личный кабинет | ⏳ 0% | ⏳ 0% | В планах |
| Админ панель | ⏳ 0% | ⏳ 0% | В планах |

**Общий прогресс: ~40%**

---

## 🎯 Следующие шаги

1. ✅ Главная страница
2. ✅ Header и Footer
3. ✅ Страницы авторизации
4. ⏳ Страница курсов
5. ⏳ Личный кабинет
6. ⏳ Админ панель
7. ⏳ Уведомления и сообщения

---

## 🐛 Известные проблемы

- **Нет проблем** - всё работает отлично! ✨

---

## 📞 Контакты

Если нужна помощь с переводами:
- Email: info@japjaryq.kz
- WhatsApp: +7 (777) 123-45-67

---

**Дата создания**: 31 декабря 2024  
**Версия**: 1.0.0

